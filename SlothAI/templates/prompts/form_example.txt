# form an example
1. We have a new pipeline with the following input_fields using the `$head_processor` processor:
"""
$head_input_fields
"""
2. Using the input fields from step 1, and having some fun with it, form a python dictionary that has each input_field a a key in a dict called ai_dict, then build an example 'value' for each key that makes sense based on the key name and keeping in mind this is for an example python or curl call for the user.
ai_dict = 