{# Use this template to drive an OpenAI keyterm processor! #}

{# NOTE: You may remove any template commented lines, such as this one. #}

{# Define the input_fields used in this template: #}
input_fields = [
  {
    'name': "text",
    'type': "string"
  }
]

{# The output_fields expected from the node processor using this template: #}
output_fields = [
  {
    'name': "keyterms",
    'type': "stringset"
  }
]

{# Define the 'extras' for number of keyterms desired  #}
extras = {
  'num_keyterms': None,
  'openai_token': None,
  'model': 'gpt-3.5-turbo'
}

{# Process the input string: #}
# complete dict task
1. The user has provided the following from an input_field named 'text':
"""
{{text}}
"""
2. You will build a python dictionary for the user. You will not do anything else.
3. The required keys for the dictionary are: keyterms.
4. The required value for keyterms is a list of strings.
5. Extract 5 keyterms from the text and return as a dictionary:
ai_dict =

