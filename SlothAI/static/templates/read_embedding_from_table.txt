{# Use this template to read from FeatureBase with SQL #}

{# Input Fields #}
input_fields = [{"name": "query_embedding", "type": "vector"}]

{# Output Fields #}
output_fields = [{"name": "chunks", "type": "strings"}]

{# Extras Fields #}
extras = {"table": None, "processor": "read_fb", "sql": "SELECT TOP(10) chunks, cosine_distance({{query_embedding[0]}}, query_embedding) AS distance FROM {{table}} ORDER BY distance ASC;"}