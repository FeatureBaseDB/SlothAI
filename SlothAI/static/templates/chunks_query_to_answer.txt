{# Use this template to drive an OpenAI answer node! #}

{# Define the input_fields used in this template: #}
input_fields = [{'name': "chunks",'type': "strings"},{'name': "query", 'type': "strings"}]

{# The output_fields expected from the node processor using this template: #}
output_fields = [{'name': "answer", 'type': "strings"}]

{# Define the 'extras' for number of keyterms desired  #}
extras = {'openai_token': None, 'model': 'gpt-3.5-turbo', 'iterate_key': "query"}

{# Process the answer #}
# complete dict task by following step by step
1. The user has provided the following query:
"""
{{query[iterate_key]}}
"""
2. To respond to the user's query, consider the following text:
"""
{% for chunk in chunks %}
  {{ chunk }}
{% endfor %}
"""
4. The required key for the dictionary is: 'answer'
5. The required value for answer is a string.
6. Answer the user query from step 1. and use the information in step 2. to return a python dictionary with the 'answer':
ai_dict = 