{# Use this template to drive an OpenAI embedding node processor! #}

{# Define the input_fields used in this template: #}
input_fields = [{'name': "texts", 'type': "strings"}]

{# Output Fields #}
output_fields = [{'name': "embeds", 'type': "vectors"}]

{# 1. Define the 'extras' for embedding vector size and model flavor - use instructor_xl for free #}
{# 2. For OpenAI atext-embedding-ada-002 embeddings, see the `Convert text to an OpenAI vector` template #}
extras = {'vector_size': 1536, 'model': 'text-embedding-ada-002', 'processor': 'embedding', 'openai_token': None}

{# The input for the vector engine is auto-created here from 'texts' sent to the pipeline. #}